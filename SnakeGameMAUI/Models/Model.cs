namespace SnakeGameMAUI.Models
{
    public abstract class Model
    {
        // For the moment this is an empty abstract class, because there are no common elements between all the Model classes
        // Still this class is needed for the abstract Controller and View classes, otherwise the inheritance wont work properly
    }
}
